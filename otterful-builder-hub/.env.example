# If the hub is served from a different origin than your production site (e.g. a Vercel preview),
# set this to the origin that actually hosts /builder/models/* and locomotion GLBs.
# Example: https://www.otterfulotters.xyz
# VITE_PUBLIC_ASSET_ORIGIN=
